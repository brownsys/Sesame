
  Executing command for `{ title }`
    cmd: { command }{{for a in args }} { a }{{ - endfor }}
    Working dir: { workdir }
    Env: {{ for e in env }}
          { e.key } = { e.value }{{ - endfor }}